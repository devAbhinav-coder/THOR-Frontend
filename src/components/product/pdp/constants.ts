/** Desktop PDP hover magnifier — shared with ProductDetailClient lens logic. */
export const PDP_MAIN_LENS_PX = 120;
/** Lower = gentler magnifier (was 2.45). */
export const PDP_MAIN_LENS_ZOOM = 1.95;
/** Inline motion preview loops this many seconds before restarting. */
export const PDP_INLINE_MOTION_LOOP_SEC = 5;
